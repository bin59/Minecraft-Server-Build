# 2026-09-01

## 文档更新：README.md + 插件清单.md

- 重写 `README.md` 为完整运维手册（约 600 行），新增章节：
  - 服务器速览、系统架构（同机/跨机双拓扑 ASCII 图）
  - 端口总表（游戏端 / QQ 联动 / 云主机三段）
  - 从零部署 8 步流程 + 12 项首次启动验收清单
  - 核心配置速查（server.properties / spigot.yml / 配置层级 / Aikar flags）
  - 插件依赖矩阵与推荐安装顺序
  - 日常运维（命令、备份策略、监控、例行维护周期表）
  - 故障排查速查表、安全基线（8 条必须项 + 凭据轮换顺序 + 网络最小化原则）
  - 资源链接、维护记录
- 重构 `插件清单.md`（附录 A）：A1 总览 / A2 核心 / A3 外置登录 / A4 已安装插件 /
  A5 外部程序 / A6 规划备选 / A7 依赖矩阵与安装顺序 / A8 目录文件 / A9 下载源 / A10 已知问题（P0/P1/P2 分级）
- 全文敏感信息脱敏为占位符（公网 IP、OPanel accessKey、EasyBot token、NapCat token、MCSM 密码等）
- 修正旧 README 中失效路径：`附录A-插件清单.md` → `插件清单.md`；`附录B-常见问题排查.md` → `常见问题/常见问题排查.md`

## 约定

- 含空格/括号的文件名做相对链接时，用 `<路径>` 尖括号包裹（如 `<21-玩家信息收集与展示/1.Plan (Player Analytics).md>`），
  用 `%20` 编码空格无效——括号仍会截断 markdown 链接。
- 文档链接自检脚本：正则提取 `[..](..)` 后 `urllib.parse.unquote` + `os.path.exists` 校验，本次跑出 README 92 链 / 插件清单 35 链全部有效。
- 锚点自检：提取 `^#{2,4}` 标题 → 小写、去标点（保留中英数字与空格连字符）、空格转 `-`，与 `](#xxx)` 比对。
  注意跨文件锚点 `xxx.md#yyy` 会被文件链接校验误报为失效，需人工确认。
- 本项目是 git 仓库（`8cf1874` 为初始提交）。删除文件前先 `git log --oneline -1 -- <file>` 确认已入库，保证可恢复。

## 合并：插件清单.md → README.md

- 按语义拆散合并，非简单拼接：
  - A1–A6 → 新增「四、组件清单」（4.1 总览 / 4.2 服务端核心 / 4.3 外置登录 / 4.4 已安装插件 / 4.5 外部程序 / 4.6 规划备选）
  - A7 依赖矩阵与安装顺序 → 并入「九、插件依赖与加载顺序」9.1/9.2
  - A8 目录与额外文件 → 并入「七、从零部署」7.2 目录结构（其他文件 / OPanel 辅助文件 / 核心配置文件清单三张表）
  - A9 下载源 → 并入「十四、资源链接」14.1/14.2/14.3
  - A10 已知问题 → 独立为「十二、已知问题」（P0/P1/P2 分级）
- 章节重编号：三文档导航 / 五系统架构 / 六端口 / 七部署 / 八配置 / 九依赖 / 十运维 / 十一排查 / 十二已知问题 / 十三安全基线 / 十四资源链接
- 删除 `插件清单.md`（已 git 提交于 `8cf1874`，可 `git restore` 恢复）
- 结果：README 932 行，文件链接与内部锚点全部校验通过（脚本同下）

## 遗留待办

- LuckPerms 主 JAR 缺失（P0），需下载 `LuckPerms-Bukkit-*.jar` 放入 `plugins/`
- 版本号未锁定：CMILib / Residence / OpenInv / spark / bStats 记录为「最新」，不利于复现
