# 2026-09-15

## 在「4-玩家信息与数据展示」新增 02-玩家位置记录（CoordinateLogger）

- 用户要求：在 `4-玩家信息与数据展示/` 下新建 `02-玩家位置记录/` 文件夹，增加 `CoordinateLogger玩家位置记录.md`，并同步更新引用。
- 已创建：`4-玩家信息与数据展示/02-玩家位置记录/CoordinateLogger玩家位置记录.md`（玩家坐标轨迹记录与回溯的部署文档；含插件定位对比、安装前置、config.yml 示意、数据字段、查询/导出、命令占位、性能与存储注意、常见问题；服务器专属值用 `<...>` 占位符标注，末尾注明需回填真实版本/路径/权限节点）。
- README 引用同步（Edit 精准替换）：
  - 分类总览表第 4 类（line 42）补 `· 02-玩家位置记录`（说明列加「坐标轨迹记录」）。
  - 第 3.5 章导航新增 `21.4 └ 玩家位置记录` → `4-玩家信息与数据展示/02-玩家位置记录/CoordinateLogger玩家位置记录.md`。
- 文档更新日期 2026-09-13 → 2026-09-15；维护记录追加 2026-09-15 一行。
- 未改动：README 插件调研清单表（lines 396-405，标注「尚未确认安装」），CoordinateLogger 是否实际安装未知，未强行加入；如需列入可另提。
- 备注：`ai写的/` 下未找到既有 CoordinateLogger 相关文件，故为全新文档而非迁移。

## 补全 CoordinateLogger 实际安装配置

- 用户回复「实际安装配置」后，先核查仓库无既有 CoordinateLogger 真实产物（仅脚手架 + Geyser 无关的 `show-coordinates` 行），再 WebSearch 确认其为 Modrinth 上 Mr_irak 的 CoordinateLogger（Bukkit/Spigot/Paper，ARR，存数据库）。
- 用 AskUserQuestion 确认三关键点：① 用 1.21.x 兼容构建（公开页列 1.20.1，需锁 1.21.x）；② 存储后端 MySQL；③ 命令前缀 `/cl`、权限 `coordinatelogger.*`。
- 将文档由占位脚手架改写为实际配置：config.yml（MySQL 形态，含 tracking-interval/data-retention-days/min-distance-to-save/verbose-logging/worlds/storage 段）、自动建表 SQL 示例、命令表（/cl 前缀）、LuckPerms 权限授予示例、MySQL 直查语句、性能与 FAQ。
- 仅数据库 host/user/password 与具体构建版本保留 `<...>` 占位；命令/权限子节点注明以 `/cl help` 与实际构建为准（未臆造）。
- README 维护记录追加 2026-09-15 第二行记录本次补全。

## 用户手动精简 CoordinateLogger 文档并嘱「更新其他引用」

- 用户自行把 `CoordinateLogger玩家位置记录.md` 内容精简为仅 `config.yml` 片段（约 17 行，458 字节）。
- 核查：文件名与路径未变（`4-玩家信息与数据展示/02-玩家位置记录/CoordinateLogger玩家位置记录.md`），故 README 引用（分类总览表第 4 类 line 42、第 3.5 章 `21.4 └ 玩家位置记录` line 160）仍有效，无需改链接。
- 仅在 README 维护记录追加 2026-09-15 第三行补记本次修订；文档更新日期本就是 2026-09-15，无需改。
- 经验：用户「更新其他引用」在路径未变时通常只需维护记录补记，不必改链接——先核查再动手。

## 新增 12 章「全息投影系统 — DecentHolograms」

- 用户要求：为 `2-玩法与玩家功能插件/12-DecentHolograms全息插件/DecentHolograms全息插件.md` 生成详细文档并更新项目。
- 该文件此前为 0 字节空文件，README 仅有的 "全息" 命中是 21 章「全息榜」(TAB/Plan/全息榜选型，与本插件无关)，故本项目此前无 DecentHolograms 文档。
- WebSearch + WebFetch 核实关键事实：作者 DecentSoftware-eu；GPL-3.0-or-later；平台 Paper/Spigot/Folia（需 Paper API，CraftBukkit 不行）；**无 ProtocolLib 依赖**；最新稳定 2.10.1（支持 1.8.9–26.2，含 1.21.11）；命令 `/dh`（1.19.4+ 另有 `/dh displays`）；config.yml 真实键：`defaults.{display-range,update-range,update-interval,lru-cache-size,height{text,icon,head,smallhead},down-origin,update-checker,click-cooldown,allow-placeholders-inside-animations,update-visibility-on-teleport,holograms-eye-level-positioning,player-skin-connection-timeout}` + `damage-display`/`healing-display`/`custom-replacements`；全息按文件存 `holograms/<name>.yml`；权限 `decentholograms.use/admin/command.<cmd>/*` + 每全息 `setpermission` 节点。
- 编写文档（约 17 个小节）：功能说明、安装前置、目录结构、config.yml 全注释、全息 YAML 结构、行类型(文字/ICON:/HEAD:/entity/block)、颜色&渐变&彩虹、动画(animations/ + #ANIMATION:)、点击交互(COMMAND/MESSAGE/TELEPORT/SOUND/PAGE)、分页、命令表、权限、PAPI&DHAPI、实战示例、性能排错(禁 /reload、display-range 省带宽、对比 HD+ProtocolLib TPS)、FAQ(迁移 HD / 基岩可见 / 文件存储 / Folia / VIP 专属)。
- README 同步：分类总览表 2 类补 `· 12-DecentHolograms`；第 3.2 章导航新增 `13 全息投影` 行（指向该 .md）。注意导航 12 已被「离线背包」占用，故 DecentHolograms 在 3.2 内顺延为 13（跨小节复用编号是本项目既有风格，3.3 另有独立 05、3.5 有 21.x）。
- 维护记录追加 2026-09-15 一行。
- 踩坑：并行两次 README 编辑时，分类总览(行40)成功、但 3.2 导航行插入首次「成功」却未落盘；改为单独重跑 Edit 才真正写入。教训：同一文件多 Edit 尽量串行或编辑后立刻 Read 复核。

## 新增 13 章「自定义死亡信息 — CustomDeathMessages」

- 用户要求：更新 `2-玩法与玩家功能插件/13-自定义死亡信息/自定义死亡信息customdeathmessages.md`（原为 0 字节空文件，同 DecentHolograms 起始状态）。
- 先查 `各种插件（待选）.md` 确认插件身份：**Modrinth 上 SicklySurgeon 的 CustomDeathMessages**（链接 modrinth.com/plugin/customdeathmessages，Paper 1.21+，命令 `/cdm broadcast message all ...`），即「整活核心」件。排除同名 GitHub sb2bg fork（命令/配置模型不同）。
- WebSearch + WebFetch 核实：作者 SicklySurgeon；MIT；平台 Bukkit/Paper/Purpur/Spigot；版本线 0.0.x → 1.x，最新 **1.3**（4 个月前，支持 1.18–26.1.x 含 1.21.x），1.2 同覆盖 1.21.x；1.x 为统一 `/cdm` 命令体系（取代旧 `/cdmreload` 等），权限 cdm.use/reload/test/editor/broadcast/bypass/message.vip/message.admin/admin。
- 编写文档（约 16 小节）：功能说明、安装前置、目录结构、config.yml 注释（epic-death-chance/cost-per-death-message/exempt-groups-from-cost/legacy-color-support/update-checker/effects-broadcast）、占位符全表（{victim}/{killer_name}/{killer_source}/{killer_displayname}/{cause}/{world}/{biome}/{x,y,z}/{time}/{distance}/{victim_weapon}/{victim_effects} + PAPI）、命令表、权限表、广播系统、实战示例、性能排错（禁 /reload、原子重载、异步发送、legacy-color）、FAQ（与同名 fork 差异/基岩可见/VIP 专属/收费/Discord 转发）。
- README 同步：分类总览表 2 类补 `· 13-自定义死亡信息`；第 3.2 章导航新增 `14 自定义死亡信息`（导航 13 已被全息投影占用，顺延 14，沿用跨小节复用编号风格）。
- 维护记录追加 2026-09-15 一行。
- 经验：同名插件歧义时，先查「待选插件」清单/既有引用锁定身份，再动手，避免写到错误的命令/配置模型。

## 新增 14 章「宠物系统 — SimplePets」

- 用户要求：更新 `2-玩法与玩家功能插件/14-宠物系统/宠物系统simplepets.md`（原为 0 字节空文件）。
- 查 `各种插件（待选）.md` 第二节确认插件身份：**GitHub brainsynder-Dev 的 SimplePets**（链接 github.com/brainsynder-Dev/SimplePets，Spigot 1.21.6–1.21.11，Java 21 for 1.20.5+/1.21.11，GPL-3.0，GemsEconomy 经济联动；与 FarPets 二选一勿同装）。
- WebSearch + WebFetch 核实：最新构建 **R5-B315**（2026-09-04）；EchoPets 继任者；单一 jar 覆盖 1.17–26.2（1.21.x 需精确支持，1.21.11 需 Java 21）；90+ 宠物类型、骑乘/帽子/多宠物/存档、改名多方式、每宠物独立 JSON、MySQL/SQLite、PlaceholderAPI、Addon 系统。
- 官方 Wiki Permissions 页确认权限方案为 **`Pet.*` 前缀**（非 simplepets.*）：`Pet.type.*`/`Pet.type.passive`/`Pet.type.hostile`/`Pet.type.<类型>`(下含 `.fly`/`.hat`/`.mount`/`.data.*`)、`Pet.PetToHat`/`Pet.PetToMount`、`Pet.name*`、`Pet.economy.bypass`、`Pet.commands.*` + `Pet.commands.<cmd>[.other]`。
- 编写文档（约 16 小节）：功能说明、安装前置（Java 版本矩阵）、目录结构、config.yml（标注"键名以生成文件为准"避免过度臆造）、命令表(/pet 子命令)、权限表(Pet.* 方案)、GemsEconomy Addon 付费、实战示例(普通/vip 权限授予、MySQL 跨服)、性能排错(Java 匹配/勿与 FarPets 同装/禁 /reload)、FAQ(三宠物插件对比/基岩可见/存储/Java/买宠物蛋)。
- README 同步：分类总览表 2 类补 `· 14-宠物系统`；第 3.2 章导航新增 `15 宠物系统`（导航 14 已被自定义死亡信息占用，顺延 15）。
- 维护记录追加 2026-09-15 一行。
