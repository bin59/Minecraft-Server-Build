# 2026-09-15 工作日志

## CoreProtect 操作手册补全
- 用户要求详细列举 `2-玩法与玩家功能插件/03-方块记录与回滚-CoreProtect/操作.md` 的各种操作。该文件原先为空（0 字节），已按官方文档（docs.coreprotect.net/commands）全面填充。
- **覆盖内容**：命令总览（`/co` 及别名 i/l/rb/rs/near/undo）、参数速查（u/t/r/a/i/e/f/#）、完整动作类型表（a:block/+block/-block、container、inventory、item、kill、spawn、session、chat、command、sign、username 等）、哈希标签（#preview/#count/#summary/#verbose/#silent/#optimize）、检查员模式、lookup/rollback/restore/undo/purge/status/reload/consumer/migrate-db 各操作、实战场景速查表、权限节点（coreprotect.*）、排错。
- **关键事实**：lookup 默认全局、rollback 默认半径 10；`u:#creeper/#tnt/#fire/#explosion` 用于实体/自然源回滚；purge 方块需 v23+，DuckDB/ClickHouse 迁移需 v25+；游戏内 purge 仅可删>30天、控制台>24小时。
- 官方文档正确域名：`docs.coreprotect.net`（不是旧 coreprotect.net/wiki）。
- README 未改动（文件已存在，仅填充内容）。

## CoreProtect 云端 MySQL → 本地主从复制同步文档
- 用户要"累积同步到本地、只同步未同步的"，选定「做法一：MySQL 主从复制」。已生成独立文档 `2-玩法与玩家功能插件/03-方块记录与回滚-CoreProtect/云端MySQL主从同步.md`。
- **核心**：云端 MySQL 主库（server-id=1, 开 binlog ROW 格式）+ 本地 Windows MySQL 从库（server-id=2, read_only, replicate-do-db）；`mysqldump --single-transaction --master-data=2` 全量初始 + `CHANGE MASTER TO`/`START SLAVE` 接增量；`SHOW SLAVE STATUS` 验证。
- 也记录了「做法二：脚本按 id 水位线增量拉」作为替代（适合纯文件归档、不受 purge 影响）。
- 用户后续还问了若干 CoreProtect 实战坑：全局 rollback 误伤自己建筑（用 `e:obsidian`/`e:#lava,#water` 排除）、`/co undo` 只撤销上一次且多次回滚会 diff 错乱变"残缺"、客户端 Litematica（投影）的 Easy Place 自动放置与 `M` 键和小地图冲突。
