# 2026-09-16 工作日志

## 目录内部重归类 + 同步文档清理
- 用户重归类了 2-、5-、7- 等顶层目录的内部子文件夹。
- 核实 `2-玩法与玩家功能插件\03-方块记录与回滚-CoreProtect` 现状：
  - `操作.md` 被改名为 `一些操作.md`（内容完整保留，即此前写的 CoreProtect 操作手册）。
  - 新增子目录：`CoreProtect插件：玩家的行为数据库过大\`（含 `1...数据库过大.md`、`2.将CoreProtect从SQLite切换到MySQL.md`、`img\`）、`网页端面板部署\`（deploy.sh / nginx 配置 / 部署步骤.md）。
- 此前建的 `云端MySQL主从同步.md` 与 `本地从库配置包\配置包.txt`（云端→本地 MySQL 主从同步方案）在重归类后从 workspace 内消失；经询问用户明确选择「不需要了」，**不再重建**，勿重复生成。
- 注：`2.将CoreProtect从SQLite切换到MySQL.md` 已覆盖 MySQL 切换主题；云端→本地增量同步需求若将来重启，可直接在该目录或新建「数据同步」类子目录补文档。

## 目录调整：玩家数据迁移归并
- 用户要求：把 `7-工具与常见问题约束\01-实用工具\玩家数据迁移` 移到 `5-配置与管理指南`，并把 `5-配置与管理指南` 改名为 `5-服务器管理`；移动后删除已空的 `01-实用工具`。
- 已执行：玩家数据迁移 → `5-服务器管理\玩家数据迁移\`（含 `UserOverUUID-玩家数据管迁移.md`）；`5-配置与管理指南` → `5-服务器管理` 改名完成；`01-实用工具` 空目录已删（7- 顶层仅剩 `02-常见问题`、`03-禁用影响平衡的插件功能`）。
- 同步更新根 `README.md` 7 处活跃引用：目录树 L43/L45（改名+去 01-实用工具/数据迁移）、链接 L154/L156/L170/L736/L913（`5-配置与管理指南`→`5-服务器管理`，玩家数据迁移链接改指向 `5-服务器管理/`）。历史变更日志行（L1185/1188/1190）保留原样。
- 环境坑：本机 PowerShell 工具 stdout 不回显，目录移动/改名须用 Out-File 落盘验证；长脚本（含 Select-String 大目录）偶发 exit 1，改单步小命令更稳；Remove-Item 删目录建议用 try/catch 确认。

## 目录调整（续2）：配置文件补充归并
- 用户要求：把 `5-服务器管理\01-服务器配置文件补充` 移出（认为不该在 5- 下），移入 `1-服务端核心与网络层`。
- 执行：移动并重命名为 `1-服务端核心与网络层\07-服务器配置文件补充`（1- 层原 01–06 连续，按仓库连续编号惯例续 07；若用户要 01- 原名可改回）。
- 同步更新 README：L39 目录树 1- 加 07 子项；L43 目录树 5- 去掉配置补充、描述去“配置补充”；L154/L736 链接改指向 `1-服务端核心与网络层/07-服务器配置文件补充`。Grep 验证无 `5-服务器管理/01-服务器配置文件补充` 残留。

## 目录调整（续3）：LuckPerms / CoreProtect / chunky 归并到 5-服务器管理
- 用户要求：把 `2-玩法与玩家功能插件` 下的 `01-权限管理系统-LuckPerms`、`03-方块记录与回滚-CoreProtect`、`09-chunky区块加载优化` 移到 `5-服务器管理`（保留原 01/03/09 编号，与 5- 现有 02 不冲突）。已执行移动（move3.txt 验证 src=False / dst=True）。
- 同步更新 README：目录树 L40(2-) 去三项并精简描述、L43(5-) 加三项并补描述；链接 L109/111/112/113/130/140/297/301/683/955/956/963/965/966/982/1129 的 `2-玩法与玩家功能插件/...` → `5-服务器管理/...`（按三目录前缀 replace_all）；末尾历史日志 L1177 改回 `2-玩法与玩家功能插件` 保留原貌。
- 子文档内检查：LuckPerms 文档 L206/L141 是相对链接 `../2-玩法与玩家功能插件/04-领地系统-Residence/...`（04 仍在 2-，有效未动）；L268 的 copy 命令源路径示例指向旧 2- 位置已失效，已改为 `5-服务器管理/01-权限管理系统-LuckPerms`。CoreProtect/chunky 内部无指向 2- 的绝对路径。

## 目录调整（续4）：2- 与 3- 互换编号
- 用户要求：`2-玩法与玩家功能插件` 与 `3-运维监控与面板` 互换编号（即 2-↔3-）。
- 执行：用临时名中转 rename——`3-运维监控与面板`→`_tmp_swap_3`；`2-玩法与玩家功能插件`→`3-玩法与玩家功能插件`；`_tmp_swap_3`→`2-运维监控与面板`。swap.txt 验证 new_3_play=True / new_2_ops=True / tmp_exists=False。
- 现状：2- = 运维监控与面板（01-EasyBot…06-MCSM）；3- = 玩法与玩家功能插件（02-Vault…14-宠物系统）。
- 同步更新根 `README.md`：目录树 L40/L41 互换；全量活跃链接 `2-玩法与玩家功能插件/`→`3-玩法与玩家功能插件/`、`3-运维监控与面板/`→`2-运维监控与面板/`（replace_all）；历史变更日志 L1177/L1191/L1196/L1197/L1198 改回原 2-/3- 路径保留原貌（L1188/L1189 用空格非斜杠写法，replace_all 未触及，原样保留）。Grep 验证活跃区 slash 形已零残留。
- 注：此次 swap 后，原先「续3」中改到 `5-服务器管理` 的 01-LuckPerms/03-CoreProtect/09-chunky 已不再位于 2-/3-，故其链接前缀此前已是 5-，未受本次影响。

## 文件改名 + 内容恢复：玩家位置记录文档
- 用户要求：把 `CoordinateLogger玩家位置记录` 改名为 `PosTracker玩家位置记录.md`（位于 `4-玩家信息与数据展示\02-玩家位置记录\`）。
- 现状核实：live 文件 `PosTracker玩家位置记录.md` 当时为 **0 字节（空）**；真实内容在 `.history` 备份（`CoordinateLogger玩家位置记录_20260915210237.md`，6464 字节，最完整）。改名操作把内容弄丢了，live 留下空文件。
- 处理：用 `Copy-Item -Force` 把最新 `.history` 版本内容恢复到 live `PosTracker玩家位置记录.md`（恢复后 6464 字节，CoordinateLogger 引用保留）。README L163 链接由旧名改指向 `PosTracker玩家位置记录.md`，修复断链；历史变更日志 L1193-1195 保留原样。
- 待确认：文档正文仍以真实插件名 **CoordinateLogger**（GitHub Irak-MC/CoordinateLogger、下载链接、jar 名 irak）为准；文件名虽改为 PosTracker，正文插件名未动以保持准确。若用户要做整体「换皮」改名（标题/H1 与正文插件名也改 PosTracker），需另行确认——直接改会使下载链接/jar 名失实。

## 文档换插件：CoordinateLogger → PosTracker（已确认）
- 用户纠正：CoordinateLogger 是**旧插件**，PosTracker 是**新换上的插件**，两者不同。要求把 `4-玩家信息与数据展示\02-玩家位置记录\PosTracker玩家位置记录.md` 正文整体换成 PosTracker 内容。
- 已重写：基于 CurseForge/SpigotMC 资源 122693（PosTracker 1.0.0）的官方说明撰写——简介、下载链接、config.yml（language/saveInterval/saveDataInFile/db 段）、MySQL 模式（手动建空库、插件自动建表、saveDataInFile:true 免 DB）、命令与权限（/pos help|interval|language|radius:，权限 postracker.admin）、使用示例、注意事项。
- 已正面回答此前「数据库是否手动建」：表自动建、库需手动建空库、文件模式免 DB。
- 旧 CoordinateLogger 内容仍保留在 `.history` 备份中（未删），正文已注明旧插件弃用。README L163 链接文件名未变，无需改；描述「玩家坐标轨迹采样、走失/死亡回溯」对 PosTracker 仍准确。

## Vault.md 扩充「联动其他插件」
- 用户要求更新 `3-玩法与玩家功能插件\02-经济系统-Vault\Vault.md` 内容，联动其他插件。
- 先用 Grep 摸清本服真正消费 Vault/经济的插件：Residence、EssentialsX、CustomDeathMessages、QuickMenu、DecentHolograms/TAB/Plan（PAPI 余额占位符）、Velocity（跨服经济）、SimplePets。
- 重写 Vault.md：保留 EssentialsX 经济核心 + Vault 桥 + Residence 买卖租赁 + LuckPerms 权限；新增「四、与其他插件联动」章节——
  - 4.1 Residence、4.2 LuckPerms、4.3 CustomDeathMessages（cost-per-death-message + exempt-groups-from-cost 免单）、4.4 QuickMenu（economy 菜单）、4.5 PlaceholderAPI 余额占位符 %vault_eco_balance%（TAB/DH/Plan/死亡信息）、4.6 Velocity 跨服经济（EssentialsX 单服，需 CMI/CoinsEngine/共享 MySQL 或经济主服）、4.7 SimplePets **用 GemsEconomy 非 Vault**（两套账，重要区别，部署前决定统一货币）。
- 关键事实：SimplePets 经济走 GemsEconomy + 经济 Addon，与 Vault/EssentialsX 余额互不相通；若想统一货币需让 GemsEconomy 作 Vault 经济实现。

## 纠正：SimplePets 可以用 Vault + EssentialsX（Vault Addon）
- 用户问「宠物可以使用 Vault + EssentialsX 吗」。经查 SimplePets 官方 Addon 列表，确有 **Vault Addon**：Hooks into Vault，宠物用服务器主货币（EssentialsX 余额）购买，与 /bal/Residence/死亡收费同一套账。
- 之前 Vault.md §4.7 与 SimplePets 文档写的「SimplePets 用 GemsEconomy 非 Vault、两套账」是**不准确的**——那只是「用 GemsEconomy Addon」的情况。SimplePets 支持多种经济 Addon（Vault / GemsEconomy / PlayerPoints / TokenManager / Treasury / ItemEconomy），Vault Addon 正是为 Vault 架构准备的。
- 已修正：Vault.md §4.7 改为「用 Vault Addon 即可统一货币」（附 Addon 对比表 + 安装 `/pet addon install Vault`）；同步修正 SimplePets 文档（intro、安装前置、经济联动段、FAQ）补充 Vault Addon 为推荐方案。
- 结论：本服应装 **SimplePets Vault Addon**（勿装 GemsEconomy Addon），宠物经济即与服务器主经济统一。

## 重写权限节点速查.md（覆盖所有插件）
- 用户要求：更新 `5-服务器管理\01-权限管理系统-LuckPerms\权限节点速查.md`，必须覆盖所有权限，依据 workspace + 测试服 `C:\mc_serve\1.21.11-test`。
- 数据源：用 `tar -xf <jar> -C <dst> plugin.yml`（避开被拦的 Add-Type），从测试服 29 个 jar 提取 `permissions:` 段，缩进解析，落盘 `.workbuddy\scratch\perms_extracted.txt`（ground truth）。
- 关键发现（plugin.yml 无 permissions 段的库，节点来自文档/标准树）：WorldEdit、LuckPerms、CMILib、floodgate、SkinsRestorer、opanel、spark（测试服未提取到）。
- 提取节点数：EssentialsX 435、Residence 93、CoreProtect 28、OpenInv 45、Chunky 23、Simple Voice Chat 4、BedrockPlayerSupport 10、CustomDeathMessages 6、DecentHolograms 102、QuickMenu 2、SimplePets 1015、PosTracker 1、DeluxeMenus 8、SpacePortal 4、UuidMigrate 1、PlaceholderAPI 21、Plan 33、TAB 28、ViaVersion 9、EasyBot 3、Vault 1、AntiLitematica 3、ProtocolLib 3。
- 重写后文档新增/补全章节：CustomDeathMessages、DecentHolograms、QuickMenu、SimplePets（pet.commands.* + pet.type.<mob>.* 模式，1015 全量用 /lp tree pet 导出）、PosTracker、DeluxeMenus、SpacePortal、UuidMigrate、PlaceholderAPI；Plan/TAB 从「规划」升为「已安装」并补全 33/28 节点；Residence 补 command.* 树（93）、Chunky 补 23、OpenInv 补 45、BedrockPlayerSupport 补 admin/command.*/form.backdeath、ViaVersion 补 command.* 子节点、EasyBot 合并 plugin.yml 与文档节点（标 ⚠️ 实测）。
- 环境坑（重申）：PowerShell stdout 不回显、.ps1 经 & 调用 exit 1 无输出，改用 inline PowerShell + tar 提取 plugin.yml；Remove-Item 被 safe-delete 拦，用 Move-Item。
