# 2026-09-13

## README 新增「文档分类总览」小节（方案 1：仅文档化）

- 用户采纳分类方案 1：不移动任何文件夹，只在 README 顶部补一张分类总览表。
- 在 `## 目录` 后新增 `## 文档分类总览`，按功能域把 01–27 编号章节 + 散装文件夹归为 A–G 七大类：
  - A 服务端核心与网络层：01/02/03/04/17/27
  - B 玩法/玩家功能插件：05/06/07/08/09/12/20/22/23/24/25/26
  - C 运维/监控/面板：10/11/13/14/15/19
  - D 玩家信息与数据展示：21
  - E 配置与管理指南：16/18
  - F 指令参考：指令大全
  - G 工具/常见问题/约束：实用工具 / 常见问题 / 禁用影响平衡的插件功能
- 用户特别要求：**10-EasyBot 由 B 类改到 C 类**（QQ 机器人联动归入运维/监控/面板）。已在表中落实，B 类不再含 10。
- 同步：目录 TOC 加 `文档分类总览` 锚点；文档更新日期 2026-09-12 → 2026-09-13；维护记录追加 2026-09-13 一行。
- 未采用方案 2（加分类前缀重命名）/ 方案 3（物理迁移），因会破坏大量相对链接。

## 执行方案 2（数字分类前缀重命名）

- 用户确认用**数字分类前缀 1–7**（非字母 A–G），平铺原地改名、不建子目录。
- 用 Python 脚本（托管版 3.13.12）批量处理：31 个顶层文件夹 `git mv` 改名，并改写 README 及交叉引用的 .md 内相对链接。
  - 编号文件夹用子串替换（完整文件夹名只出现在路径，安全）；4 个无编号文件夹（指令大全/实用工具/常见问题/禁用影响平衡的插件功能）用正则只替换「作为路径组件（其后紧跟 / " )）」的实例，避免误改正文描述。
  - `27-Velocity 多服` / `指令大全` / `实用工具` 三个目录 git mv 报「source directory is empty」（内部文件未提交、git 不跟踪），已回退 `os.rename` 完成改名，git 侧视为 delete+add。
  - 漏网 1 处：README 第 958 行用 URL 编码 `22-Simple%20Voice%20Chat`（脚本只匹配空格形式），已手动改为 `2-22-Simple%20Voice%20Chat`。
  - 全仓 grep 复查：`1-03-.../Geyser-Floodgate/README.md` 内 `01-Geyser-Spigot.md` / `02-Floodgate.md` 是同目录内部文件引用（非顶层文件夹），属误报，不动。
- 分类总览表由 A–G 改为数字 1–7 并填入带前缀新名；维护记录追加 2026-09-13 方案 2 行。
- 临时脚本 `_plan2_rename.py` 执行后已删除。

## 执行方案 3（建分类目录 + 物理迁移 + 去冗余前缀）

- 用户确认分类目录命名：「数字分类 + 去冗余前缀」。新建 7 个分类目录：1-服务端核心与网络层 / 2-玩法与玩家功能插件 / 3-运维监控与面板 / 4-玩家信息与数据展示 / 5-配置与管理指南 / 6-指令参考 / 7-工具与常见问题约束。
- Python 脚本批量处理 31 个文件夹：`os.makedirs` 建分类目录后 `git mv`（失败回退 `os.rename`）移入并去掉冗余数字前缀（`1-01-服务器核心-Leaf` → `1-服务端核心与网络层/01-服务器核心-Leaf`）。
- 同时重写所有 .md/.txt 内相对链接：编号文件夹子串替换，4 个无编号文件夹路径组件替换；另加 `2-22-Simple%20Voice%20Chat` 的 %20 编码变体（否则 URL 编码链接会失效）。
- 分类总览表由扁平 `1-01-...` 改为分层树（分类目录 / 子文件夹），更清爽。
- 全仓 grep 复查：仅 Geyser-Floodgate/README.md 内 `01-Geyser-Spigot.md`/`02-Floodgate.md` 是同目录内部文件引用（非顶层路径），属误报，不动。
- 临时脚本 `_plan3_migrate.py` 执行后已删除。

## 修正不连续编号（分类内连续重编号）

- 用户要求「修正不连续的目录标题名」，确认方式：每个分类内从 1 重新连续编号（如 1-服务端核心与网络层 由 01/02/03/04/17/27 → 01–06）。
- Python 脚本 `_fix_renumber.py`：rename_map 仅列出实际改名的 27 个子文件夹（分类1前4个已连续未动），git mv（失败回退 os.rename）改名，并把旧完整路径映射写入 link_map 批量改写 .md 链接；另加 `22-Simple%20Voice%20Chat` 的 %20 变体。
- 新编号：cat1 01–06、cat2 01–12、cat3 01–06、cat4 01、cat5 01–02、cat6 01-指令大全、cat7 01–03。采用 2 位零填充与原始风格一致。
- 分类总览表手动改写为连续编号（脚本只改完整路径链接，表格用短名故需手动同步）。
- 校验：全仓 grep 旧子文件夹名（17-/27-/05-/10-/21-/16- 等）仅命中方案2历史维护文字，无失效链接。
- 临时脚本 `_fix_renumber.py` 执行后已删除。

## 移动 12-快捷菜单系统 到 ai写的 + 改引用

- 用户要求把 `12-快捷菜单系统` 从 `2-玩法与玩家功能插件/` 移到顶层 `ai写的/`，随后要求「改引用」。
- 先移动（git mv 失败回退 mv）：`2-玩法与玩家功能插件/12-快捷菜单系统/` → `ai写的/12-快捷菜单系统/`（5 项内容齐全）。
- 改写全部引用（用 Edit 精准替换，无脚本）：
  - README 分类总览表（第 40 行）去掉 `· 12-快捷菜单系统` 条目（已不在 2 类下）。
  - README 第 26 章 4 行链接前缀 `2-玩法与玩家功能插件/12-快捷菜单系统/` → `ai写的/12-快捷菜单系统/`（130/131/132/133）。
  - `1-服务端核心与网络层/06-Velocity 多服/Velocity 多服.md:477` 相对链接 `../2-.../12-快捷菜单系统/` → `../../ai写的/12-快捷菜单系统/`。
  - `ai写的/12-快捷菜单系统/快捷菜单系统-QuickMenu.md` 内目录树（150/151）与 `cd` 示例（482）路径同步更新。
- 全仓 grep `2-玩法与玩家功能插件/12-快捷菜单系统` 零残留；剩余 `快捷菜单系统` 命中均为新路径或章节标题/维护文字（无失效链接）。
- 备注：`build.ps1:14/61` 仍写「26-快捷菜单系统」目录名（旧章节号，移动前即如此），属脚本注释、非路径链接，未改；如需统一可另提。
- 维护记录追加 2026-09-13 一行。

## 重命名 12-快捷菜单系统 → 快捷菜单系统 + 改引用

- 用户要求去掉冗余 `12-` 前缀：把 `ai写的/12-快捷菜单系统/` 重命名为 `ai写的/快捷菜单系统/`（git mv 失败回退 mv）。
- 改写全部路径引用（Edit 精准替换）：README 第 26 章 4 行 `ai写的/12-快捷菜单系统/` → `ai写的/快捷菜单系统/`；Velocity 多服.md `../../ai写的/12-快捷菜单系统/` → `../../ai写的/快捷菜单系统/`；文档内目录树与 `cd` 示例路径同步去掉 `12-`。
- 全仓 grep `12-快捷菜单系统` 仅剩维护记录历史行（保留移动当时旧名以忠实记录）；其余零残留。
- 维护记录新增一行记录此次重命名。

## 移动「各种插件（待选）」到仓库最外层

- 用户要求：把 `各种插件插件（待选）` 文件夹移到最外层（顶层），并修改相关引用。
- 原路径：`7-工具与常见问题约束/01-实用工具/各种插件插件（待选）/`（内有 `各种插件（待选）.md`）。
- 用 `git mv`（失败回退 `mv`）将其移至仓库根目录 `各种插件插件（待选）/`，成为独立顶层目录。
- README 引用更新：仅 §3.6 实用工具 一处（`7-工具与常见问题约束/01-实用工具/各种插件插件（待选）/各种插件（待选）.md` → `各种插件插件（待选）/各种插件（待选）.md`）。
- 全仓 grep `各种插件插件（待选）` 复查：仅命中更新后的 README 第 165 行，无残留旧路径。
- 维护记录追加 2026-09-13 一行。
