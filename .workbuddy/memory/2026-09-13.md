# 2026-09-13 工作日志

## CoreProtect 云端 MySQL → 本地从库配置包
- 用户本地已装 MySQL，要把云端 CoreProtect 库增量同步到本地（累积 + 只同步未同步的）。
- 因不能远程连其私有 MySQL，改为生成「开箱即用配置包」：`2-玩法与玩家功能插件/03-方块记录与回滚-CoreProtect/本地从库配置包/配置包.txt`
  - 6 步可运行：找 my.ini → 追加 server-id=2 / relay-log / replicate-do-db → 重启 MySQL 服务 → 导入 co_full.sql → CHANGE MASTER TO + START SLAVE → SHOW SLAVE STATUS\G 验证。
  - 用户只需填 3 个值：<云主机IP>、<repl密码>、<coreprotect库名>；第 4 个 binlog 位点由 `co_full.sql` 顶部的 `-- CHANGE MASTER` 注释自动带出，不用手填。
- 在 `云端MySQL主从同步.md` 末尾增「十二、本地已有 MySQL 的实操」章节引用配置包，并补充两点本地注意：
  - server-id 冲突：本地 MySQL 若本身当过主库（原 server-id=1）要改成不同唯一值。
  - read_only 取舍：从库建议 read_only=1 防误写；若本地还跑别的写入应用则保持注释，replicate-do-db 已限定只重放 CoreProtect 库。
